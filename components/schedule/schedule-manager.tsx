"use client";

import { FormEvent, useState, useTransition } from "react";
import { CalendarDays, ChevronLeft, ChevronRight, Plus, X } from "lucide-react";
import type { ScheduleEvent } from "@/lib/db/domain-types";
import { createScheduleEventAction } from "@/app/actions/schedule";

type CalendarView = "week" | "day" | "month";
const initialDate = new Date(2026, 8, 9);
const dayFormatter = new Intl.DateTimeFormat("fr-FR", { weekday: "long", day: "numeric", month: "long" });
const monthFormatter = new Intl.DateTimeFormat("fr-FR", { month: "long", year: "numeric" });
const shortDayFormatter = new Intl.DateTimeFormat("fr-FR", { weekday: "short" });
const monthDayFormatter = new Intl.DateTimeFormat("fr-FR", { day: "numeric" });
function getWeekDays(date: Date) { const monday = new Date(date); monday.setDate(date.getDate() - ((date.getDay() + 6) % 7)); return Array.from({ length: 7 }, (_, index) => { const day = new Date(monday); day.setDate(monday.getDate() + index); return day; }); }
function getMonthDays(date: Date) { const firstDay = new Date(date.getFullYear(), date.getMonth(), 1); const start = new Date(firstDay); start.setDate(1 - ((firstDay.getDay() + 6) % 7)); return Array.from({ length: 42 }, (_, index) => { const day = new Date(start); day.setDate(start.getDate() + index); return day; }); }
function sameDay(first: Date, second: Date) { return first.toDateString() === second.toDateString(); }

export function ScheduleManager({ initialEvents }: { initialEvents: ScheduleEvent[] }) {
  const [events, setEvents] = useState(initialEvents);
  const [open, setOpen] = useState(false);
  const [view, setView] = useState<CalendarView>("week");
  const [selectedDate, setSelectedDate] = useState(initialDate);
  const [error, setError] = useState<string | null>(null);
  const [isPending, startTransition] = useTransition();
  const weekDays = getWeekDays(selectedDate); const monthDays = getMonthDays(selectedDate); const sortedEvents = [...events].sort((first, second) => first.time.localeCompare(second.time));
  const periodLabel = view === "day" ? dayFormatter.format(selectedDate) : view === "month" ? monthFormatter.format(selectedDate) : `${weekDays[0]?.getDate() ?? 0} - ${weekDays[6]?.getDate() ?? 0} ${monthFormatter.format(selectedDate)}`;
  function movePeriod(direction: number) { const nextDate = new Date(selectedDate); if (view === "day") nextDate.setDate(selectedDate.getDate() + direction); if (view === "week") nextDate.setDate(selectedDate.getDate() + direction * 7); if (view === "month") nextDate.setMonth(selectedDate.getMonth() + direction); setSelectedDate(nextDate); }
  function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const formData = new FormData(event.currentTarget); startTransition(async () => { const result = await createScheduleEventAction({ title: formData.get("title"), time: formData.get("time"), type: formData.get("type") }); if (!result.ok) { setError(result.error); return; } setEvents((current) => [...current, result.event]); setOpen(false); setError(null); event.currentTarget.reset(); }); }
  function eventCard(event: ScheduleEvent) { return <div className={`event-block event-${event.type.toLowerCase()}`} key={event.id}><b>{event.title}</b><small>{event.type} · Famille Martin</small></div>; }
  return <><div className="schedule-actions"><button type="button" className="primary-button" onClick={() => setOpen(true)}><Plus size={17} /> Nouvel événement</button></div><div className="calendar-toolbar"><button type="button" className="icon-button calendar-nav-button" onClick={() => movePeriod(-1)} aria-label="Période précédente"><ChevronLeft size={18} /></button><strong>{periodLabel}</strong><button type="button" className="icon-button calendar-nav-button" onClick={() => movePeriod(1)} aria-label="Période suivante"><ChevronRight size={18} /></button><button type="button" className="today-button" onClick={() => setSelectedDate(new Date(initialDate))}>Aujourd&apos;hui</button><div className="view-switch">{([["week", "Semaine"], ["day", "Jour"], ["month", "Mois"]] as const).map(([value, label]) => <button type="button" className={view === value ? "active" : ""} key={value} onClick={() => setView(value)}>{label}</button>)}</div></div>{view === "week" && <div className="week-day-picker">{weekDays.map((date) => <button type="button" className={sameDay(date, selectedDate) ? "active" : ""} key={date.toISOString()} onClick={() => setSelectedDate(date)}><small>{shortDayFormatter.format(date).replace(".", "")}</small><b>{date.getDate()}</b></button>)}</div>}{view === "day" && <section className="calendar-card calendar-day"><div className="calendar-day-header"><CalendarDays size={18} /><span>{dayFormatter.format(selectedDate).split(" ")[0]}</span><b>{dayFormatter.format(selectedDate).slice(dayFormatter.format(selectedDate).indexOf(" ") + 1)}</b></div><div className="day-timeline">{sortedEvents.map((event) => <div className="event-row" key={event.id}><time>{event.time}</time>{eventCard(event)}</div>)}</div></section>}{view === "week" && <section className="calendar-card calendar-week"><div className="week-grid-header">{weekDays.map((date) => <button type="button" className={sameDay(date, selectedDate) ? "selected" : ""} key={date.toISOString()} onClick={() => setSelectedDate(date)}><small>{shortDayFormatter.format(date).replace(".", "")}</small><b>{date.getDate()}</b></button>)}</div><div className="week-grid-body">{weekDays.map((date) => <div className={`week-grid-column ${sameDay(date, selectedDate) ? "selected" : ""}`} key={date.toISOString()}>{sameDay(date, selectedDate) && sortedEvents.map(eventCard)}</div>)}</div></section>}{view === "month" && <section className="calendar-card calendar-month"><div className="month-grid-header">{["Lun", "Mar", "Mer", "Jeu", "Ven", "Sam", "Dim"].map((day) => <span key={day}>{day}</span>)}</div><div className="month-grid">{monthDays.map((date) => { const inMonth = date.getMonth() === selectedDate.getMonth(); const selected = sameDay(date, selectedDate); return <button type="button" className={`month-cell ${inMonth ? "in-month" : "outside-month"} ${selected ? "selected" : ""}`} key={date.toISOString()} onClick={() => setSelectedDate(date)}><span>{monthDayFormatter.format(date)}</span>{selected && sortedEvents.slice(0, 2).map((event) => <em key={event.id}>{event.time} · {event.title}</em>)}</button>; })}</div></section>}{open && <div className="modal-backdrop"><form className="modal-card" onSubmit={submit}><button type="button" className="modal-close" onClick={() => setOpen(false)} aria-label="Fermer"><X size={18} /></button><p className="eyebrow">Planning familial</p><h2>Nouvel événement</h2><label>Titre<input name="title" required placeholder="Ex. Rendez-vous médical" /></label><div className="form-columns"><label>Heure<input name="time" type="time" required /></label><label>Type<select name="type"><option>Présence</option><option>Enfants</option><option>École</option><option>Repas</option><option>Autre</option></select></label></div>{error && <p className="form-error">{error}</p>}<button className="primary-button" type="submit" disabled={isPending}>{isPending ? "Enregistrement..." : "Enregistrer l'événement"}</button></form></div>}</>;
}
